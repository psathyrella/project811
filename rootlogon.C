{
  gROOT->ProcessLine(".x $CMSSW_BASE/src/MitHzz4l/macros/MitStyleRemix.C");
  // TString path(gSystem->GetIncludePath());
  // path += " -I "+TString(getenv("ROOFITSYS"))+"include";
  // gSystem->SetIncludePath(path);
}
